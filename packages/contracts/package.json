{
  "name": "@myweather/contracts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Wire-format types shared between @myweather/api and @myweather/web. The API ships these on the response body; the web app reads them.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
